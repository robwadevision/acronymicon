/**
 * Acronymicon — constants.example.js
 * Template for local credentials. Copy this file to constants.js and fill in your values.
 * constants.js is gitignored and must never be committed.
 */

const AcronymConfig = {
  measurementId: "", // GA4 measurement ID, e.g. "G-XXXXXXXXXX"
  apiSecret: "",     // GA4 Measurement Protocol API secret
};
